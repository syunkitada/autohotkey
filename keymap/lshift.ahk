; row 1
<+q::
	if !operate("+q")
		Send +q
	Return

<+w::
	if !operate("+w")
		Send +w
	Return

<+e::
	if !operate("+e")
		Send +e
	Return

<+r::
	if !operate("+r")
		Send +r
	Return

<+t::
	if !operate("+t")
		Send +t
	Return

<+y::
	if !operate("+y")
		Send +y
	Return

<+u::
	if !operate("+u")
		Send +u
	Return


<+i::
	if !operate("+i")
		Send +i
	Return

<+o::
	if !operate("+o")
		Send +o
	Return

<+p::
	if !operate("+p")
		Send +p
	Return


; row 2
<+a::
	if !operate("+a")
		Send +a
	Return

<+s::
	if !operate("+s")
		Send +s
	Return

<+d::
	if !operate("+d")
		Send +d
	Return

<+f::
	if !operate("+f")
		Send +f
	Return

<+g::
	if !operate("+g")
		Send +g
	Return

<+h::
	if !operate("+h")
		Send +h
	Return

<+j::
	if !operate("+j")
		Send +j
	Return

<+k::
	if !operate("+k")
		Send +k
	Return

<+l::
	if !operate("+l")
		Send +l
	Return


; row 3
<+z::
	if !operate("+z")
		Send +z
	Return

<+x::
	if !operate("+x")
		Send +x
	Return

<+c::
	if !operate("+c")
		Send +c
	Return

<+v::
	if !operate("+v")
		Send +v
	Return

<+b::
	if !operate("+b")
		Send +b
	Return

<+n::
	if !operate("+n")
		Send +n
	Return

<+m::
	if !operate("+m")
		Send +m
	Return

RShift:: Send {sc029}

>+Space::
	Send {F10}

; row 1
>+q::
	if !operate("+q")
		Return
	Return

>+w::
	if !operate("+w")
		Return
	Return

>+e::
	if !operate("+e")
		Return
	Return

>+r::
	if !operate("+r")
		Return
	Return

>+t::
	if !operate("+t")
		Return
	Return

>+y::
	if !operate("+y")
		Return
	Return

>+u::
	if !operate("+u")
		Return
	Return


>+i::
	if !operate("+i")
		Return
	Return

>+o::
	if !operate("+o")
		Return
	Return

>+p::
	if !operate("+p")
		Return
	Return


; row 2
>+a::
	if !operate("+a")
		Send 1
	Return

>+s::
	if !operate("+s")
		Send 2
	Return

>+d::
	if !operate("+d")
		Send 3
	Return

>+f::
	if !operate("+f")
		Send 4
	Return

>+g::
	if !operate("+g")
		Send 5
	Return

>+h::
	if !operate("+h")
		Send 6
	Return

>+j::
	if !operate("+j")
		Send 7
	Return

>+k::
	if !operate("+k")
		Send 8
	Return

>+l::
	if !operate("+l")
		Send 9
	Return

>+;::
	if !operate("+;")
		Send 0
	Return

>+sc028::
	if !operate("+:")
		Send 0
	Return

; row 3
>+z::
	if !operate("+z")
		Return
	Return

>+x::
	if !operate("+x")
		Return
	Return

>+c::
	if !operate("+c")
		Return
	Return

>+v::
	if !operate("+v")
		Return
	Return

>+b::
	if !operate("+b")
		Return
	Return

>+n::
	if !operate("+n")
		Return
	Return

>+m::
	if !operate("+m")
		Return
	Return
